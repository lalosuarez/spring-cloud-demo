spring:
  profiles: default
  application:
    name: eureka-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false
      username: configserver
      password: configserver1

---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://192.168.99.100:8888
      fail-fast: false
      username: configserver
      password: configserver1